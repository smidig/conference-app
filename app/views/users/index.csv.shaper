csv.headers :name, :tlf, :company, :twitter, :accepted_optional_email, :allergies, :role, :ticket, :includes_dinner, :completed

csv.rows @users do |csv, user|
  csv.cells :name, :tlf, :company, :twitter, :accepted_optional_email, :allergies, :role, :includes_dinner
  csv.cell :ticket, user.ticket.name
  csv.cell :completed, user.completed?
end