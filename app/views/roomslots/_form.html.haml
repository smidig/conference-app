= simple_form_for @roomslot, :html => { :class => 'form-horizontal' } do |f|
  = f.association :room
  = f.association :timeslot
  = f.association :talks

  .actions
    = f.button :submit, :class => 'btn-primary'