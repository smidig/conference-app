= render "details"

%h3 Legg til deltaker på din bestilling

= simple_form_for @user, :html => {:class => 'form-vertical'},  :url => url_for(:action => 'add_user', :controller => 'orders') do |f|
  = f.association :ticket, :collection => Ticket.visible, :label_method => :display, :selected => Ticket.default.id
  = f.input :email, :autofocus => true, :required => true
  = f.input :name
  = f.input :tlf
  .form-actions
    = f.button :submit, :class => 'btn-primary', :value => "Legg til deltaker"
    = link_to t('.cancel', :default => t("Avbryt")), {:action => "show", :id => @order.id}, :class => 'btn'