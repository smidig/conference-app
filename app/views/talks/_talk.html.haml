%h3
  =talk.title
  %span.pull-right
    =link_to "Slett", talk, confirm: 'Are you sure you want to delete this talk?', :method => :delete, :class => 'btn btn-danger'
    =link_to "Edit", edit_talk_path(talk), :class => 'btn btn-success'
%span.author
  =talk.user.name
%p.description
  =talk.description
-if talk.presentation.file?
  =link_to "Last ned", talk.presentation.url, :class => 'btn btn-warning'