
= simple_form_for @timeslot, :html => { :class => 'form-horizontal' } do |f|
  = f.input :start
  = f.input :end
  = f.input :is_workshop_slot

  .actions
    = f.button :submit, :class => 'btn-primary'
